// prisma/schema.prisma
datasource db {
  provider = "postgresql"
  url      = env("NEON_PRISMA_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Comments {
  id    Int     @id @default(autoincrement())
  comment   String
}